/**
 * @deprecated This module is no longer used.
 * The client now calls the extractor directly instead of going through an HTTP API.
 * Kept as an empty placeholder to avoid breaking any stale imports.
 */
